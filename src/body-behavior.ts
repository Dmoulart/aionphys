/**
 * The body behaviors define the way the body react to other bodies.
 *
 */
export enum BodyBehaviors {
    /**
     * - Dynamic bodies are affected by gravity and can move.
     */
    Dynamic,

    /**
     * - Static bodies are not affected by gravity and cannot move.
     */
    Static
}
